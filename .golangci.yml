run:
  timeout: 5m

linters:
  enable:
    - errcheck
    - govet
    - ineffassign
    - staticcheck
    - unused
    - gofmt
    - goimports
    - misspell
    - unconvert
    - unparam

linters-settings:
  goimports:
    local-prefixes: github.com/whoaa512/asana-cli

issues:
  exclude-use-default: false
  max-issues-per-linter: 0
  max-same-issues: 0
